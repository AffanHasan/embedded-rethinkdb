package com.etilize.embedded.rethinkdb;

public class Test {

	// should detect if database for a specific version is already there
	// if not present then should download sources in user home
	// should build OS specific binaries
	// should start on free port
	// should stop
}
